{
  "kind": "condpool",
  "seed": 42,
  "k": 16,
  "n_blocks": 3,
  "d_block": 512,
  "d_model": 256,
  "attention_hidden_dim": 128,
  "dropout": 0.1,
  "gamma_init": 0.0,
  "gamma_trainable": true,
  "batch_size": 64,
  "lr": 0.0005,
  "weight_decay": 0.001,
  "max_epochs": 200,
  "patience": 20,
  "grad_clip": 1.0,
  "selection": "minimum validation RMSE, including unchanged baseline at epoch 0",
  "global_branch": "frozen original predictor, never replaced by residue mean",
  "target": "global + gamma * target_std * local_member; per-member standardized MSE",
  "additional_seeds": false
}
